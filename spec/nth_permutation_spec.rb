RSpec.describe NthPermutation do
  it 'has a version number' do
    expect(NthPermutation::VERSION).not_to be nil
  end

  it 'return valid version number' do
    expect(NthPermutation::VERSION).to eq('0.1.0')
  end
end
